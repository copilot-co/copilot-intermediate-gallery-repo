# Optional path to the JSON data file used for persistent gallery data.
GALLERY_DATA_PATH=./data/gallery-data.json

# Base URL used to resolve object-storage media keys into public URLs.
# For local development this can stay as '/'.
GALLERY_MEDIA_BASE_URL=/
