## ✅ Step 1 checked — nice work!

Your baseline workflow is in. Here's Step 2.

## Step 2: Classify delegable vs. human steps

Reply to **this issue** with a comment that copies your Step 1 list and tags each step `[Delegate]` or `[Human]`, with a one-line reason for each — for example:

```
- [Delegate] Implement the component — boilerplate, follows existing GalleryGrid patterns
- [Human] Final review and merge — need to confirm it matches product intent
```

> [!IMPORTANT]
> Keep at least one step tagged `[Human]`. An agentic redesign is about choosing what to delegate, not delegating everything.

The bot checks new comments on this issue for both tags.
